[tool.ruff]
target-version = "py311"
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
# UP042: str+Enum -> enum.StrEnum changes __str__ output, risky for schemas
# serialized as API responses; not worth it for a lint-only pass.
ignore = ["UP042"]
